{% extends 'base.html.twig' %}

{% block title %}Gestion des élèves{% endblock %}

{% block body %}

    <h1>Page de gestion des élèves</h1>

    <div id="eleves">
        {% for niveau in niveaux %}
            <div class="niveau">
                <h2 class="carlito-bold">{{ niveau.nom }}</h2>
                <div>
                    {% for classe in niveau.classes %}
                        <div class="classe">
                            <h2 class="carlito-bold">{{ classe.nom }}</h2>
                            <div>
                                {% for eleve in classe.eleves %}
                                    <a href="{{ path('app_eleve_show',{id: eleve.id})}}"><p class="carlito-regular" id="eleve">{{ eleve.prenom }} <b>{{ eleve.nom }}</b></p></a>
                                {% endfor %}
                            </div>
                        </div>
                    {% endfor %}
                </div>
            </div>
        {% endfor %}
    </div>

    <script>
        document.addEventListener("DOMContentLoaded", function () {
            document.querySelectorAll(".toggle").forEach(function (toggleElement) {
                toggleElement.addEventListener("click", function () {
                    const content = this.nextElementSibling;
                    if (content) {
                        content.style.display = content.style.display === "none" ? "block" : "none";
                    }
                });
            });
        });
    </script>
{% endblock %}
